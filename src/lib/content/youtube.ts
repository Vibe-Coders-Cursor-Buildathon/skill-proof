/**
 * YouTube transcript extraction.
 * Implementation pending — Phase 1 (P3).
 */

export async function fetchYouTubeTranscript(url: string): Promise<string> {
  void url;
  throw new Error("fetchYouTubeTranscript is not implemented");
}
