/**
 * Web article fetch and text extraction.
 * Implementation pending — Phase 1 (P3).
 */

export async function fetchArticleContent(url: string): Promise<string> {
  void url;
  throw new Error("fetchArticleContent is not implemented");
}
