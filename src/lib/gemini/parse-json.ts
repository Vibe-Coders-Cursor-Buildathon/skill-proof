/**
 * Extract and parse JSON from Gemini model output.
 * Implementation pending — Phase 1 (P2).
 */

export function parseJsonFromModel<T>(text: string): T {
  void text;
  throw new Error("parseJsonFromModel is not implemented");
}
